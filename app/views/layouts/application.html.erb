<!DOCTYPE html>
<html>
  <head>
    <title>Pairbnb</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width">

    <link href="https://fonts.googleapis.com/css?family=Poppins:400,600" rel="stylesheet">
    <!-- Include Semantic UI -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/1.11.8/semantic.min.css"/>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/1.11.8/semantic.min.js"></script>

    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.5.0/css/all.css" integrity="sha384-B4dIYHKNBt8Bc12p+WXckhzcICo0wtJAoU8YZTY5qE0Id1GSseTk6S+L3BlXeVIU" crossorigin="anonymous">



    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>

      <div class="ui segment">
        <div class="ui secondary menu pointing" style="border:0;">
          <div class="item">
            <%= link_to root_url do %>
              <%= image_tag "/images/airbnb_logo.PNG", alt: "airbnb-logo", class: "ui image", style: "width:34px; height: auto; "%>
            <% end %>
          </div>
          <div class="item">
            <div class="ui icon input">
              <i class="search link icon"></i>
              <input type="text" placeholder="Search..." class="navbar-search">
            </div>
          </div>
          <div class="right menu">
            <%= link_to "Add Listing", new_listing_path, class: "item" %>
            <a class="item">
              Host
            </a>
            <a class="item">
              Saved
            </a>
            <a class="ui item">
              Trips
            </a>
            <a class="ui item">
              Messages
            </a>

            <% if signed_in? %>                
              <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "ui item" %>
            <% else %>
              <%= link_to "Sign In", new_user_session_path, class: "ui item" %>
              <%= link_to "Sign Up", new_user_registration_path, class: "ui item" %>
            <% end %>

            <a class="ui item">
              $20 Credit
            </a>
            <a class="ui item img">
              <img src="/images/profile_pic.jpg" alt="profile-pic" class="ui circular image mini">
            </a>
          </div>
        </div>
      </div>

    <div class="container">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
